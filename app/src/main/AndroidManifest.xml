<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finance_Tracker"
        tools:targetApi="31">
        <activity
            android:name=".activity_move"
            android:exported="false" />
        <activity
            android:name=".activity_mines"
            android:exported="false" />
        <activity
            android:name=".activity_plus"
            android:exported="false" />
        <activity
            android:name=".ui.activities.Return_password"
            android:exported="false" />
        <activity
            android:name=".ui.activities.RegistrationPage"
            android:exported="false" />
        <activity
            android:name=".ui.activities.LoginPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.HomePage"
            android:exported="false" />
    </application>

</manifest>